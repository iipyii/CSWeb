version: "3.8"

services:

  db:
    image: pgvector/pgvector:pg17
    container_name: csweb_dbvector
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Cssusu5555+
      POSTGRES_DB: csweb_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./backend/init.sql:/docker-entrypoint-initdb.d/init.sql

  # backend:
  #   build: ./backend
  #   container_name: csweb_backend
  #   platform: linux/amd64 # 👈 เพิ่ม
  #   restart: always
  #   ports:
  #     - "5000:5000"
  #   depends_on:
  #     - db
  #   environment:
  #     DB_HOST: db
  #     DB_USER: postgres
  #     DB_PASSWORD: Cssusu5555+
  #     DB_NAME: csweb_db
  #     DB_PORT: 5432

  # frontend:
  #   build: ./frontend
  #   container_name: csweb_frontend
  #   platform: linux/amd64 # 👈 เพิ่ม
  #   restart: always
  #   ports:
  #     - "5173:5173"
  #   depends_on:
  #     - backend

volumes:
  pgdata:
